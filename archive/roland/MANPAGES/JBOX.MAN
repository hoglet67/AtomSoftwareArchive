JBOXThe JBOX utility rom is originally written by Jan Bronswaer as a utility kit that provides a set of useful commands for debugging and memory tools. It contains the following statements:`BIN         `BIT       `CAS`DAS         `FVAR      `FZER`HELP        `HIP       `HVAR`HXD         `INFO      `LAB`LIB         `LINE      `MFIND`PACK        `PLIST     `RAM`TRACE       `TURBO     `USR`VER         `WP        `XMSThe statements TURBO, WP and XMS are only for Atom2k15.