P-CHARME An interpreter for structured `BASIC by Frans van Hoesel. `AUTO     `FUNCTION  `PROC`BEEP     `HEX       `PROGRAM`BSAVE    `HTAB      `READ`CASE     `ICOPY     `RESTORE`CAT      `INKEY     `RENUM`CONT     `INSTR     `STOP`COPY     `KEY       `TRUE`COS      `NOT       `VAR`DEL      `ODD       `VTAB`ELSE     `ON        `WEND`EVEN     `PAUSE     `WHILE`FALSE    `PEND      `XIF`FEND     `POP       `ZERO