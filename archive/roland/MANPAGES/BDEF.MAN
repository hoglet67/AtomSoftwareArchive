VersionAddress: #BDEF or #9FEFDefault value: #12The lower nibble is the minor version number. This will be incremented for backwards compatible changes, such as the addition of new features that leave existing feature unchanged.The upper nibble is the major version number. The will be incremented for non-backwards compatible changes, such as changes to existing feature.Patch releases (i.e. bug fixes) that do not affect software compatibility will not change the version number.